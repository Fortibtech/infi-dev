version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - yarn install
      - npx prisma generate
      - yarn build
run:
  runtime-version: 20.11.0
  command: node dist/main
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: "production"
